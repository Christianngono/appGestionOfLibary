<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Accueil</title>
    <link rel="stylesheet" href="/public/static/index.css">
</head>
<body>
    <h1><%= page %></h1>

    <div id="books">
        <% books.forEach(book => { %>
            <div class="book">
                <h2><%= book.title %></h2>
                <p>Auteur: <%= book.author %></p>
                <p>Genre: <%= book.genre %></p>
                <p><%= book.borrowed ? 'Emprunté' : 'Disponible' %></p>
                <p>Date d'échéance: <%= book.dueDate %></p>
            </div>
        <% }); %>
    </div>

    <div class="actions">
        <a href="/addBook">Ajouter un livre</a>
        <a href="/deleteBook">Supprimer un livre</a>
        <a href="/borrowBook">Emprunter un livre</a>
        <a href="/returnBook">Retourner un livre</a>
    </div>
</body>
</html>